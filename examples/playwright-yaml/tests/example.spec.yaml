name: Example Tests
url: https://demo.playwright.dev/todomvc/

config:
  timeout: 180000

tests:
  - name: Sample test
    tags: [smoke]
    steps:
      - Navigate to the home page
      - Add a new todo item "Learn OpenQA"
      - Verify that "Learn OpenQA" appears in the todo list
